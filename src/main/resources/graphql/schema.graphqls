type Query {
    members: [Member]
    member(id: Int!): Member

    products: [Product]
    product(id: Int!): Product

    exception: Boolean
}

type Mutation {
    createMember(member: MemberRequest): Member
}